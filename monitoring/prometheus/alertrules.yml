groups:
  - name: api_alerts
    rules:
      - alert: ApiDown
        expr: up{job="precificador-api"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "API inacessivel"
          description: "O target precificador-api esta com up == 0."
